# tasks.md

今後の TODO を、実装しやすい順で整理します。README に将来仕様を混ぜないための退避先としても使います。

## 優先度高

- mobile import の重複取り込み検知を追加する
- `--advice` で読み出したログを振り返りに活用する方法を設計する
- README のセットアップを CI 相当で定期確認できるようにする

## 優先度中

- `--advice` の次段階として、AI による助言生成の責務を定義する
- `mirror-logs` の存在意義を定義する
- `mobile-inbox` のサブディレクトリ運用を定義する
- `--output-dir` の利用例をテストケースとして増やす
- `--help` の文言を README と定期突合できるようにする
- 空メモ運用の扱いを見直す

## 優先度低

- `mental-auto` をローカルインストールで呼びやすくする手順を README に追加する
- ログ例を増やす
- 英語の簡易補足を README 冒頭に追加する

## 仕様検討メモ

### mobile-inbox

決めること:

- 同一ファイルの重複取り込みをどう避けるか
- `## Mobile notes` 内の区切りを足すか
- archive 先で同名衝突したときどうするか
- 空 Markdown をどう扱うか

### `--advice`

現在:

- 第一段階として、最新の日次ログを読み込んで stdout に表示する機能は実装済み
- AI による助言生成はまだ行わない

次に決めること:

- 読み出したログをどのように振り返りへ活用するか
- ルールベースか LLM ベースか
- 入力対象を最新ログだけにするか、複数日のログへ広げるか
- advice を保存するか、その場で出すだけか
### `mirror-logs`

決めること:

- Markdown からの派生 JSON にするか
- 正本を `logs` に置くか `mirror-logs` に置くか
- 更新タイミングを保存時同期にするか、後処理にするか

## 変更時の完了条件

- 実装がある
- テストがある
- README の例が更新されている
- `docs/design.md` の責務説明が更新されている
